# Fed-ICL Replication — Dependencies
# ===================================
# Install with: pip install -r requirements.txt

# ── Core (required to run the project) ────────────────────
requests>=2.28.0          # HTTP client for Ollama REST API

# ── Data & Computation (stdlib — no install needed) ───────
# numpy is used but comes pre-installed with most Python setups
# If you get ImportError, uncomment the line below:
numpy>=1.24.0

# ── Future: Dissertation Experiments ──────────────────────
# Uncomment these when you need them:
# sentence-transformers>=2.2.0   # SBERT embeddings for KATE-style selection
# datasets>=2.14.0               # HuggingFace datasets (SST-2, AG News)
# matplotlib>=3.7.0              # Plotting convergence curves
# scikit-learn>=1.3.0            # Evaluation metrics (F1, precision, recall)
# flwr>=1.5.0                    # Flower federated learning framework
